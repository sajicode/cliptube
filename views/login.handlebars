<section id="main-section">
  <div class="container">
    <div class="dark-overlay">
      <div class="main-inner container">
        <div class="row">

          <div class="col-md-6 mx-auto">
            {{#if successMsg}}
            <div class="alert alert-success">{{successMsg}}</div>
            {{/if}}

            {{#if errorMsg}}
            <div class="alert alert-danger">{{errorMsg}}</div>
            {{/if}}

            {{#if error}}
            <div class="alert alert-danger">{{error}}</div>
            {{/if}}

            <!--login modal-->
            <div id="login-modal">
              <div class="card bg-success text-center card-form modal-body">
                <div class="card-body">
                  <h3>Teacher Login</h3>
                  <form action="/login" method="POST">
                    <div class="form-group">
                      <div class="form-group">
                        <input type="text" class="form-control form-control-lg" placeholder="Username" name="username">
                      </div>
                      <div class="form-group">
                        <input type="password" class="form-control form-control-lg" placeholder="Password" name="password">
                      </div>
                      <input type="submit" value="Submit" class="btn btn-outline-class btn-block">
                    </div>
                  </form>
                  <p>Don't have an account? <a href="/register" id="modal-register">Register here</a></p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
